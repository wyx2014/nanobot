# Skills

The following skills extend your capabilities. Each listed `SKILL.md` path is exact: when using a listed skill, call `read_file` on that path directly. Do not call `list_dir` or search to locate a listed skill; inspect its directory only when its SKILL.md explicitly requires another file.
Unavailable skills need dependencies installed first — you can try installing them with apt/brew.

{{ skills_summary }}
